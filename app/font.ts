import { Inconsolata, Playfair_Display, Courgette } from 'next/font/google';

export const inconsolata = Inconsolata({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const playfairdisplay = Playfair_Display({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const courgette = Courgette({
  weight: ['400'],
  subsets: ['latin'],
});
